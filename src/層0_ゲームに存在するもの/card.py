"""
層0：カード定義

カードが持つことができる情報の種類のみを定義する。
具体的な効果内容、数値の意味（「攻撃力」「コスト」など）は上位層で定義される。

詳細仕様は CARD_SPEC.md を参照してください。
"""

# 層0カード仕様（詳細はCARD_SPEC.mdを参照）
# - 名前：型: 文字列
# - 種類：型: 文字列
# - 状態：型: 有り・無し（具体的な状態名は上位層で定義）
# - 数値情報：型: 可変数の数字を配列で保持（意味は上位層で定義）
# - 分類情報：型: 可変数の文字を配列で保持（意味は上位層で定義）
# - 効果：型: 有り・無し（具体的な効果内容は上位層で定義）
